# 扩写 brief（每一对章节的 agent 都必须完整读这份，读完再动手）

## 〇、你在做什么、为什么

这本书原来是 40 章（现代数学 IMO 拔尖生课程：复数 → 流形 → 拓扑 → 泛函分析/量子力学
→ Lie 群 → 范畴/同调代数/代数几何 → Galois 理论）。读者反馈：**推导已经不跳步、不
手挥了，但对"认真的普通自学者"来说，从一个定义到下一个定理之间还是跳得太快**——
一步里悄悄用了好几条前提，一个记号背后压了好几次具体运算。

解决方案：**把每一章拆成两章**，40 章 → 80 章。拆分已经在基础设施层面做完了：

- 原来的 40 章内容**原样保留**在偶数号章节（`chNN_..._下.md`），标题、导航、编号
  （`\tag{}`、`第 XX 章` 交叉引用）都已经改写成新的 80 章编号，**不需要你再管这些**。
- 每个偶数章前面新增了一个**空骨架**奇数章（`ch(NN-1)_..._上.md`），七节标题都在，
  等你填。

你分到的是**一对**：`chXX_..._上.md`（骨架，待填）+ `ch(XX+1)_..._下.md`（已有内容，
待加细）。两个文件都要交付。

## 一、这是什么（沿用原书定位，读者群做了调整）

| | |
|---|---|
| **内容来源** | 知乎专栏《数学物理连载》的重写课程本身（不是原始专栏文章——本沙箱环境**没有**原始专栏文本和 opc2 知识库，你唯一可靠的原始素材就是已经写好的「下」半章内容） |
| **课程形态** | 数学进阶课程，仍然严格、不回避证明、不省中间步骤 |
| **目标读者** | 从「IMO 拔尖生」放宽为**认真的普通自学者**——愿意动笔跟着算，但不能替他们脑补任何一步。深度不降，**推导的颗粒度要更细**。 |
| **深度锚** | 仍对标俄罗斯数学物理高中（СУНЦ МГУ / ФМШ 57 中一类），但"预备"章可以适当放慢，先给具体数字例子再抽象化 |

## 二、「上」章怎么写（新写，七节骨架已建好，逐节填）

**核心原则：先具体，后抽象；先手算，后证明。**「上」章不是"简化版"，**结论一条都
不能少于「下」章需要用到的量**——它只是换了个顺序：把「下」章里"压缩成一步"的东
西，用 2–4 个具体数字/具体小例子先算一遍，让读者在看到一般定义之前，已经**用手**
做过这件事。

### 各节要求

**一、本章概要**——这一章要打磨哪个具体的"跳跃点"（读「下」章那半，找出哪几步对
普通读者太快，本章就是为了那几步做准备）；三五句话说完。

**二、入口：一道具体的问题**——可以是「下」章入口题的**简化/预热版**（数字更小、
条件更具体），也可以是同一道题但本章只要求"算出答案/猜出规律"，不要求给出一般证明
（证明留给「下」章）。

**三、结构：定义与完整推导**——本章主体，也是最重要的一节。具体做法：
1. 挑出「下」章里 2–4 个"跳步"最明显的定理/定义（比如一步里同时用了三条前提、
   或者一个记号背后压了好几次代数运算）。
2. 对每一个，**先代入 2–3 组具体数字/具体对象，手算一遍**，把「下」章压缩的步骤
   拆回三五步；每一步都写清楚在算什么、为什么这样算。
3. 算完具体例子后，**再**抽象成一般的陈述——这时候读者已经知道这个陈述"为什么
   是对的"，一般证明只是把手算的步骤符号化。
4. 记号第一次出现时要说清楚**为什么需要这个记号**（不要凭空丢出一个新符号）。

**四、几何与物理直觉**——用更贴近日常经验的类比（不要求专业背景），为「下」章的
几何/物理直觉section 做铺垫。

**五、经典问题精讲**——2–4 道题，比「下」章的对应节更基础，逐题给完整解。

**六、练习**——同样三档 + **完整解答**，题目难度整体比「下」章低一档。**解答一样
不能跳步、不能"留给读者"。**

**七、Takeaway 与延伸**——3–5 条核心洞察；结尾**明确交棒**给「下」章，例如：
"现在你已经能手算出 XX 和 YY 了；下一章会证明这对任意情形都成立，并给出一般结构。"

### 「上」章的字数与篇幅

正文目标 **5,000–9,000 字**（中文计，含练习解答）——比「下」章略短是正常的，因为
它是铺垫，不必重复「下」章的完整深度。

## 三、「下」章怎么加细（已有内容，你在原有基础上编辑，不是重写）

**不要删除、不要弱化任何一条已有的定义/定理/证明/结论。** 你的任务是**加**，不是
"重写得更好"。具体做法：

1. **通读一遍**，标出哪些步骤是"一行带过三个操作"的地方（常见形式：一个等式后面
   跟一句"直接验证"、"代入化简即得"、"整理得"，但中间省略了 2 步以上的代数）。
2. 对每一处，**插入中间步骤**：把"代入化简即得 X"展开成"代入得 ...；用 ... 恒等式
   整理左边...；两边同减 ...；得到 X"。可以用 `**（展开）**` 或直接续写的方式插入，
   不新增小节标题，保持原有的定义/定理编号不变（插入的中间步骤不需要单独编号）。
3. 在每个主要定义/定理**给出之前**，补一句"为什么要这样定义/为什么会想到证这个"
   的动机句（如果原文已经有，不用重复写）。
4. **本章概要（一）** 的开头加一句衔接语，明确指向配套的「上」章，例如：
   "读完第 XX 章的具体例子后，这里把同样的构造写成一般定义并给出完整证明。"
5. 如果你在「上」章里新引入了某个记号/说法，"下"章第一次用到时要保持**同一个**
   记号（两章互相对齐，读者从上读到下不能有断层）。

**「下」章加细后的字数**：应该比原文**增加**，目标 **8,000–14,000 字**（原文大多在
7,000–12,000 字区间，加了中间步骤后自然会变长）。

## 四、你可以读的素材

### 1. 「下」章的现有内容（**唯一可靠的原始依据**）

就是分给你的那个 `ch(XX+1)_..._下.md` 文件本身——**先完整读一遍**。它已经是严格、
不跳步的版本；你从里面提炼"哪几步对普通读者还是太快"，以及"上"章要复现哪些结论。

### 2. 专家依据（数学正确性的参考）

```
_experts/
├── algebra/          （含 _SKILL.md 人格+方法论）
├── analysis/
├── numbertheory/
└── combinatorics/
```

每个目录的 `_SKILL.md` 是方法论；其余 `*.md` 是定理/公式/例子，可用来补充「上」章
需要的具体计算细节或补充例子。你分到的那一章文件头部 `专家依据:` 那一行已经指明了
最相关的文件，优先读那个。

### 3. 本沙箱**没有**原专栏文本、**没有** opc2 知识库

`_BRIEF.md`（原始 brief）里提到的 `/home/pc/yysdl/...` 路径在这里都不存在，**不要
尝试读取**，遇到就跳过，不影响你写作——「下」章的现有内容已经包含了你需要的一切
数学实质。

## 五、⚠️ 数学排版铁律（与原 `_BRIEF.md` 完全一致，违反就是返工）

这书用 **kramdown + MathJax**（GitHub Pages）。规则：

| ❌ | ✅ | 原因 |
|---|---|---|
| `$x_i$`（单美元） | `$$x_i$$` | kramdown 只认 `$$` |
| `$$P(a\|b)$$`（裸竖线） | `$$P(a \mid b)$$` 或 `\vert` | 非 display 行里的 `\|` 会吐空表 |
| `$$\|x\|$$` | `$$\lvert x\rvert$$` | 绝对值 |
| `$$\|S\|$$` | `$$\lvert S\rvert$$` | 基数 |
| `\|\|x\|\|` | `\lVert x\rVert` | 范数 |
| 正文 `A \| B` | `A \\| B` | 正文字面竖线也会触发幽灵表格 |
| `$10` | `\$10` | 未转义的 `$` 开启幽灵数学段 |
| `\mathopen\|` / `\mathclose\|` | `\vert` | Dirac 括号高频陷阱 |
| `\big\|` / `\Big\|` / `\bigg\|` / `\Bigg\|` | `\big\vert` 等 | 限制映射/求值/取模 |
| `f\|_{N^\perp}` | `f\vert_{N^\perp}` | 限制映射的竖线 |

另外三条硬约束：

1. **全书公式一律 `$$...$$`，行内也是。** 不出现单个 `$` 包数学。
2. **禁止 `{{` 和 `{%`**——Jekyll Liquid 在 Markdown 之前解析，代码块和公式里也一样。
   单花括号 `{x : x > 0}` 没问题；两个连续花括号必须写 `\{` 或包进
   `{% raw %}...{% endraw %}`。写完后 `grep -n '{{' 你的文件` 必须为空。
3. **文件首行绝对不能是 `---`**（会被当 YAML frontmatter）。骨架文件已经从
   `---\nlayout: default\n---\n\n# 第N章...` 开始，**不要改动这个开头结构**。

```python 围栏内不受这些限制，字面 $ 和 | 随便写。
```

## 六、术语归一

`docs/glossary.md` 是唯一真值源，写之前先读一遍，逐字复用里面的译法。约定：
**现代数学专有名词用「拉丁/英文写法 + 中文对照」**（`Galois 群` 不写 `伽罗瓦群`，
`Zariski 拓扑` 不写 `扎里斯基拓扑`）。首次出现给 `中文 (English)` 形式。

需要新增术语：**不要改 glossary.md**（共享文件，避免并发冲突）。在交付说明里列出你
新引入的术语，由主控统一合并。

## 七、编号约定（与原 `_BRIEF.md` 完全一致）

定理/定义/引理/推论/命题一律 **`节号.条号`**（如 `定义 3.1`），只写两段，不写三段，
不用章号。计数器按节内出现顺序递增；同一单元里定义与定理/引理/推论共享同一个号；
「例」「注」用紧邻前一条的号，自身单调。号必须单调不减。

**跨章引用必须带（新的）章号**：写「第 10 章的 定义 3.23」。你分到的「下」章文件里
的交叉引用**已经改写成新编号**，你只需要保持这个约定，不需要再核对旧编号。**新写的
「上」章如果要引用「下」章的定理，用配套那一章的新编号**（骨架文件头部已经写明了
配套章节的编号）。

### 公式编号

只在正文要引用时才编号，用 **`\tag{章.序}`**——**章用新编号**（骨架文件头部已注明
本章的新编号）。「上」章如果需要编号公式，从 `\tag{本章新编号.1}` 开始独立计数，不
要接着「下」章的编号走。

## 八、⚠️ 别用 Python 脚本写章节（LaTeX 会被吃）

用脚本写文件时字符串必须加 `r` 前缀，否则 `\t \b \f` 会被解释成控制符，`\times`、
`\big` 静默损坏（肉眼看不出来）。**更稳的做法：直接用 Write / Edit 工具改写文件**，
不要绕脚本。

## 九、⚠️ 骨架已经建好，你只需要填内容/加细，不需要重建骨架

- 「上」章：骨架（frontmatter、标题、元信息、七节标题、导航块）**已经是最终形态**，
  不要改标题、不要改导航块、不要改元信息四行。你只需要把每节的 `（待填：...）`
  占位替换成真正内容。
- 「下」章：**不要动 frontmatter、标题行、`配套预备` 行、元信息四行、结尾导航块**——
  这些已经改好了新编号。你只在七节正文内部加细。

## 十、硬约束

- **只写你分到的这一对文件**（`chXX_..._上.md` 与 `ch(XX+1)_..._下.md`）。不要动
  `docs/` 下其他章、不要动 `glossary.md`、`index.md`、`_sidebar.md`、`_navnames.md`
- **不要提交 git**，不要 `git add` / `git commit`
- **不要删除任何文件**
- 平台是 Linux，`grep` / `python3` 可用

## 十一、写完必须自查

```bash
python3 _check.py docs/chXX_..._上.md
python3 _check.py docs/ch(XX+1)_..._下.md
```

两个文件都要跑，都要显示「结果: 全过 ✓」（中文字数警告 `⚠` 不算失败，但字数明显
不足要自己补）。

## 十二、交付时说明

对**每个**文件分别说明：
1. 文件路径
2. 正文字数
3. `_check.py` 的实际输出（结果行）
4. 你新引入的术语（供主控合并进 glossary）
5. 你**没把握**的地方（数学正确性存疑的地方，宁可标出来，不要糊过去——这比字数
   更重要）
