# 章节写作 brief（每一章的 agent 都必须完整读这份）

你在参与重写一本书：**《数学物理连载 · 重写课程》**。

## 一、这是什么

| | |
|---|---|
| **内容来源** | 知乎专栏《数学物理连载》（作者 菜场学院菜老师 @jRONI），179 篇文章，编号 MP1–MP149 + 旁支 |
| **课程形态** | 数学奥赛课程（**不是**大学教材的直接翻译，也不是科普） |
| **目标读者** | **竞赛生里的拔尖者** —— IMO 候补水平、或准备大学先修的高中生 |
| **深度锚** | **俄罗斯数学物理高中**（СУНЦ МГУ / ФМШ 57 中一类）。特点是：严格、不回避证明、早引入分析工具、从具体到抽象 |
| **架构** | C · 螺旋重写 —— 章序照原专栏的链，但每章用奥赛题做入口与练习 |

**一句话定位**：内容确实是这位老师的现代数学链条，形态确实是奥赛课。这条链的终点比俄罗斯物理数学高中的终点高两个学段——**难度梯度就靠「入口题」和「三档练习」铺**。

## 二、每章的固定结构（七节，一节不能少）

```markdown
# 第N章: 中文标题 (English Title)
> 对应原专栏: MPxx–MPyy
> 专家依据: <见本批次指定>
> 知识库依据: <见本批次指定>
> 深度锚: 对标俄罗斯物理数学高中（СУНЦ МГУ 级）

## 一、本章概要 (Overview)
## 二、入口：一道具体的问题 (Entry Problem)
## 三、结构：定义与完整推导 (Structure & Proof)
## 四、几何与物理直觉 (Intuition)
## 五、经典问题精讲 (Classical Problems)
## 六、练习 (Exercises)  —— 基础 / 竞赛 / 研究 三档
## 七、Takeaway 与延伸 (Takeaways)
```

### 各节的要求

**一、本章概要** —— 这一章要解决什么核心问题；与前后章的逻辑关系（从哪来、到哪去）。**三五句话说完，不要铺陈。**

**二、入口：一道具体的问题** —— **先给题，不给定义。**
- 这是全书最重要的设计：俄罗斯数学圈的做法是**从问题里长出结构**，不是先讲抽象定义再补例子
- 题目来源要标注：奥赛真题 / Квант / 教材经典例题 / 自编
- 这道题必须**用本章后面的工具才能真正解掉** —— 它是全章的引子，不是装饰

**三、结构：定义与完整推导** —— 这一节是主体。
- 关键定义给中英对照：`中文 (English)`
- **完整推导，不跳步、不手挥。** 严禁出现「显然」「易证」「不难看出」而不给理由
- 每个定理给出**证明思路**再给证明本身
- 如果某一步需要引用本课后面的结论，明确标出「见第 XX 章」

**四、几何与物理直觉** —— 这个东西在几何上长什么样、在物理里对应什么。
- 本课程贯穿的主线是**「几何 ↔ 物理 ↔ 代数」三者的对应**，每章尽量点出这条线

**五、经典问题精讲** —— 3–5 道经典题，**逐题给完整解**。每题标注：考点、它在本章结构里的位置。

**六、练习** —— 三档题面 + **完整解答**（见下）

**⚠️ 所有练习题都必须给出完整解答。这是本课的设计原则，不是可选项。**

理由：这是 agent 时代的课程。读者随时能问 AI 要答案，**藏答案只会制造「黑盒」**——
读者卡住时无法自查、无法确认自己是真懂还是以为懂。把解答写全，读者才能真正自学。

#### 题面（三档，梯度要真的拉开）

- **基础**（巩固定义，3–4 题）：做完就能确认自己读懂了定义
- **竞赛**（本课目标难度，4–5 题）：奥赛风格，需要组合本章工具 + 一点巧思
- **研究**（通向下一章，2 题）：**留悬念，但同样给解答**——解答末尾点明「这正是第 XX 章的起点」

题面用 **`**基1.**` / `**竞1.**` / `**研1.**` 编号**（不要用 markdown 有序列表，
以免和后面的解答段落串起来）。

#### 解答（紧跟在题面之后）

固定写成 `### 解答 (Solutions)` 子节，按同一套编号逐题给**完整解**：

- **不跳步、不手挥**，和正文推导一个标准。禁止「显然」「易证」「留给读者」
- 计算题给**完整步骤与最终结果**；证明题给**完整证明**（可先一句「思路」再展开）
- 竞赛档的解答要**点出这题的关键 leap 是什么** —— 让读者看清「卡点在哪、怎么想到的」
- 研究档的解答末尾必须有一句**接到下一章**
- 解答里如果需要引用本章的定义/定理，写明编号（如「由定理 3.5」）

示例格式（**照抄这个结构**）：

```markdown
### 基础（巩固定义）

**基1.** 把下列复数写成 $$a+bi$$ 形式：……

**基2.** 证明……

### 竞赛（本课目标难度）

**竞1.** ……

### 研究（通向下一章）

**研1.** ……

### 解答 (Solutions)

**解 基1.** 逐个算。……（完整步骤）……

**解 基2.** 思路：……。证明：……。$$lacksquare$$

**解 竞1.** 关键 leap 在于……。完整证明：……

**解 研1.** ……（完整解答）。注意这与第 03 章的「对偶」是同一件事的两面——那正是下一章的起点。
```

**七、Takeaway 与延伸** —— 3–5 条核心洞察；下一章的悬念；延伸阅读。

### 章末必须有导航块

**⚠️ 邻居文件名不要猜 —— 去查 `/home/pc/yysdl/psmath-book/_navnames.md`。**

那张表列了全书 40 章的标准文件名与前后邻居，**逐字照抄**。
（已经有 4 处断链是因为 agent 自己猜邻居文件名：ch11/ch13/ch19/ch21 各猜错一次，比如写成 `ch20_Banach代数与C星代数.md`、`ch22_..._Neumann定理.md`。）


```html


---

<!-- chapter-nav -->
<div style="display:flex; justify-content:space-between; align-items:center; padding:1em 0;">
  <div><a href="chXX_prev.md">← 第XX章 上一章标题</a></div>
  <div><a href="index.md">↑ 目录</a></div>
  <div><a href="chXX_next.md">第XX章 下一章标题 →</a></div>
</div>
```

首章左侧 `<div></div>` 留空；末章右侧留空。

## 三、⚠️ 数学排版铁律（**违反就是全书返工**）

这书写在 GitHub Pages 上，用 **kramdown + MathJax**。kramdown 的解析和你的编辑器**不一样**：

| ❌ 会被 kramdown 弄坏 | ✅ 正确写法 | 原因 |
|---|---|---|
| `$x_i$`（单美元） | `$$x_i$$` | **kramdown 只认 `$$`**。单 `$` 不是数学，`_` 会变成 `<em>`，下标全毁 |
| `$$P(a\|b)$$`（裸竖线） | `$$P(a \mid b)$$` 或 `\vert` | 非 display 行里的 `\|` 会让 kramdown 吐出一张**空表** |
| `$$\|x\|$$`（绝对值） | `$$\lvert x\rvert$$` | 具名定界符不含竖线字符 |
| `$$\|S\|$$`（基数） | `$$\lvert S\rvert$$` | 同上 |
| 范数 `\|\|x\|\|` | `\lVert x\rVert` | 同上 |
| 正文里的 `A \| B` | `A \\| B` | 正文里的字面 `\|` 也会触发幽灵表格 |
| 货币 `$10` | `\$10` | 未转义的 `$` 会开启一个幽灵数学段 |
| **`\mathopen\|` / `\mathclose\|`** | `\vert` | **Dirac 括号的高频陷阱**：`\langle\psi\mathopen\|\psi\rangle` 里那个 `\|` 是 MathJax idiom，但**输出就是字面竖线**，kramdown 照样吐空表。写成 `\langle\psi\vert\psi\rangle` |
| `\big\|` / `\Big\|` / `\bigg\|` / `\Bigg\|` | `\big\vert` … | 限制映射、求值、取模都常用；**这些变体一律要换** |
| `f\|_{N^\perp}`（限制） | `f\vert_{N^\perp}` | 限制映射的竖线也是竖线 |

### 另外三条硬约束

1. **章内公式一律 `$$...$$`，行内公式也是 `$$`。全书不出现单个 `$` 包数学。**
2. **禁止 `{{` 和 `{%`** —— Jekyll 的 Liquid 在 Markdown **之前**解析，**代码块和公式里也一样**。
   - 集合写法 `{x : x > 0}` 没问题（单花括号），但**两个连续花括号**（如 Poisson 括号 `{{H, W}, W}`）必须写成 `\{` 或包进 `{% raw %}...{% endraw %}`
   - 写完后自查：`grep -n '{{' 你的文件` 必须为空
3. **章首第一行绝对不能是 `---`** —— Jekyll 会当成 YAML frontmatter，构建直接失败。文件从 `# 第N章: ...` 开始。

### 代码块里不受这些限制

```python 围栏内**不适用**数学/竖线规则，字面 `$` 和 `|` 随便写。

## 三点五、⚠️ 术语归一：**先数，再选**

同一概念有多种中文写法时（如「Galois 群」vs「伽罗瓦群」、「Hilbert 空间」vs「希尔伯特空间」）：

**先在全库 grep 数一遍，以出现次数多的那一方为正条**，不要把少数派写法当标准。

**本书已定的约定**：**现代数学专有名词用「拉丁/英文写法 + 中文对照」**——

```
✅ Zariski 拓扑 / Hilbert 零点定理 / Heisenberg 代数 / Galois 群 / Hilbert 空间
❌ 扎里斯基拓扑 / 希尔伯特零点定理 / 海森堡代数 / 伽罗瓦群 / 希尔伯特空间
```

（首次出现处给 `中文 (English)` 形式，如「Zariski 拓扑 (Zariski topology)」；
若确要提中文旧译，写成「也称扎里斯基拓扑」这类**对照注**，不要当作正条。）

> **今天有两次栽在这上面**：
> - ch38 的 agent 把「Galois」单方面改成了「伽罗瓦」（实际 Galois 114 : 伽罗瓦 29，而且 ch39 的章标题就是 Galois）
> - glossary 的 agent 自己数出了「正文多以拉丁写法拼组（共 167 处）」，**却仍然选了「伽罗瓦」归一**——因为它读到的是 ch38 那份**已经被改错**的文件
>
> 教训：**读到一个写法，不等于那是主流写法。** 数完再定。

## 四、术语

`/home/pc/yysdl/psmath-book/docs/glossary.md` 是**唯一真值源**。写之前先读它，逐字复用里面的译法。
需要新增术语，**在你自己的章节里首次出现处给 `中文 (English)`**，并在提交说明里列出新增项（不要改 glossary.md，由主控统一合并）。

## 五、你可以读的素材

### 1. 原专栏正文（**主要依据**）

```
/home/pc/yysdl/zhihu-psmath/src/<id>_<标题>.txt
```

按 MP 号找：`ls /home/pc/yysdl/zhihu-psmath/src/ | grep 'MP1_'`（注意 `MP1_` 与 `MP10_` 不冲突）。

带 LaTeX 结构的 HTML 副本在 `/home/pc/yysdl/zhihu-psmath/src_html/<id>.html`（公式还原更准时用它）。
编号对照表在 `/home/pc/yysdl/zhihu-psmath/_list.json`。

### 2. 专家依据（四个数学专家工具的参考文档）

```
/home/pc/yysdl/psmath-book/_experts/
├── algebra/          （含 _SKILL.md 人格+方法论）
├── analysis/
├── numbertheory/
└── combinatorics/
```

每个目录里的 `_SKILL.md` 是**方法论与陷阱清单**（值得先读）；其余 `*.md` 是具体领域的定理/公式/例子。

### 3. 知识库依据（内容覆盖，章级笔记）

```
/home/pc/yysdl/opc2/knowledge/math/...  和  .../physics/...
```

本批次会指定具体路径。**这些是章级 md，直接读相关章节即可**，不要通读整个目录。

## 五点三、⚠️ 别用 Python 脚本写章节（LaTeX 会被吃）

如果要用脚本往文件里插内容，**字符串必须加 `r` 前缀**：

```python
# ✗ 会坏：\t \b \f 被解释成控制符
f.write("$$\\times$$ 与 $$\\big$$")
# 实际写入的是 "$$<TAB>imes$$" 和 "$$<退格>ig$$" —— LaTeX 静默损坏

# ✓ 正确
f.write(r"$$\times$$ 与 $$\big$$")
```

**更稳的做法：直接用 Write / Edit 工具改写文件**，不要绕脚本。

> 第 08 章的 agent 就踩了这个：`\times`/`\to` 变成制表符、`\big`/`\blacksquare` 变成退格符，
> 肉眼完全看不出来（在编辑器里就是个空白）。它自己是靠「控制字符扫描 + 逐行 `$$` 奇偶校验」发现的。
> `_check.py` 第 7 项现在会查这个。

## 五点四、⚠️ 编号约定（**全书必须统一**）

定理/定义/引理/推论/命题一律写成 **`节号.条号`**，例如：

```markdown
## 三、结构：定义与完整推导

**定义 3.1（线性泛函, linear functional）** …      ← 本章第 3 节的第 1 条
**引理 3.1（延拓引理）** …                        ← 同一个"单元"内，类型不同可以同号
**定理 3.2（对偶基的存在与唯一性）** …             ← 第 3 节的第 2 条
```

规则：

1. **只写两段**（`3.1`），**不写三段**（~~`3.1.1`~~），**也不要用章号**（~~`12.1`~~）。
2. 计数器按**节内出现顺序**递增；**同一个单元**里的定义与定理/引理/推论**共享同一个号**
   （定义 3.1 与 定理 3.1 并存是对的）。
   **「例」和「注」用「与紧邻前一条相同的号」**——即跟它所属的那条定义/定理共享号：

   ```
   **定理 3.6（…）** …          ← 结果
   **注 3.6（…）** …            ← 关于它的一条注
   **例 3.6（…）** …            ← 关于它的一个例
   ```

   …但如果它与前一条确实无关（只是顺次补一个例子），就取**下一个可用的号**。
   **关键是「例/注」的号必须自身单调**，不要出现 `例 3.15` 后面又冒出 `例 3.2`。

   > 全书已有的写法不止一种（ch15/ch24 用「与紧邻前一条同号」，ch09 用「填空号」，
   > ch03 另起一套）。**新写的章节按上面这条来**，不必回头改旧章——检查器只要求各自单调。
3. **号必须单调不减**：正文里 `定义 3.2` 不能出现在 `引理 3.1` 之前。
   若发现顺序冲突，**改号，不要挪位置**（挪位置会破坏「先定义后引用」，比如某引理说的「上述级数」就是指它前面那条定义）。
4. **跨章引用必须带章号**：写「第 05 章的 定义 3.23」，不要只写「定义 3.23」。
5. 正文里**回指**某条时写成 `**定理 3.31 说**：…` 的手挥格式没问题，但别用 `**定理 3.31（…）**` 的完整格式，否则会被机械检查当成重复陈述。

> ⚠️ 这条是事后补的：前 14 章里已经出现三套写法（`3.1` / `8.1.1` / `12.1`），
> 正在回改。**余下章节一律按本节执行。**

### 公式编号

**只在正文要引用某个公式时才编号**；不需要引用的公式一律不编（大多数属于此类）。

需要编号时用 **`\tag{章.序}`** —— 这是教科书的通用惯例：

```markdown
$$d^2=0 \tag{12.5}$$
```

- `(12.5)` = **第 12 章**的第 5 个编号公式（不是第 12 节的第 5 个）
- 正文引用写 `由 (12.5)`；引用别章写 `第 07 章的 (3.5)`
- **不要用字母**（~~`(A)(B)`~~），**不要用无前缀的 `(1)(2)`**（跨章引用会歧义）

> **为什么条目用「节.条」而公式用「章.序」？**
> 两者是**两个独立的命名空间**，各按自己的惯例即可：
> 条目（定义/定理）用 `节.条` 便于「在本文里就近定位」；
> 公式用 `章.序` 是出版惯例，且用括号 `(12.5)` 与类型词 `定理 3.1` 天然区分。
> ch12 已经这么写（`\tag{12.1}`–`\tag{12.19}` + 55 处引用），**不用改**。

## 五点五、⚠️ 先建骨架再填肉（**这条是血的教训**）

**第一步就用 Write 建一个只含七节标题 + 章末导航块的骨架文件**，然后再逐节 `Read` 素材、`Edit` 填入。

**不要先把所有素材读完再动笔。** 第 03 章的第一个 agent 就是这么挂的：

```
读满 396 KB 上下文 → 输出 "I have everything I need. Now writing the chapter."
→ 说完就结束，文件从未被创建，整整一轮白跑
```

正确节奏：

```
① Write  → 骨架（七节标题 + 导航块），几十行，立刻落盘
② Read   → 只读本章需要的素材（brief 明确说「不要通读整个目录」就真的别通读）
③ Edit   → 一节一节填，每填完一节文件都已经是完整的
④ 卡住也不会全丢  —— 骨架在盘上，接手的人（或你自己下一轮）能接着填
```

**判据**：如果你发现自己连续 Read 了 5 个以上文件还没开始写，**停下，先去建骨架。**

## 六、硬约束

- **只写你自己那一个文件**。不要动 `docs/` 下别人的章、不要动 `glossary.md`、不要动 `index.md` / `_sidebar.md` / `_config.yml`
- **指定什么路径就写什么路径**，不要改名
- **不要提交 git**，不要 `git add` / `git commit`
- **不要删任何文件**（共享目录，有误清空前科）
- 篇幅目标：**正文 6,000–12,000 字**（中文计）——**因为练习解答占了相当篇幅**。低于 5,000 字说明推导或解答缩水了
- 平台是 Linux，`pdftotext` / `grep` / `python3` 可用

## 七、写完必须自查

```bash
python3 /home/pc/yysdl/psmath-book/_check.py docs/<你的章节文件>.md
```

逐条打印：单美元 / **数学跨度内**裸竖线 / Liquid 冲突 / 首行 `---` / 七节齐全，
外加 `$$` 是否成对、中文字数。

> ⚠️ **不要用裸 `grep` 自查那条竖线规则** —— 跨单元格匹配会把**每个 markdown 表格行都误报**。
> `_check.py` 只检查 `$$...$$` 之内。第一版 brief 栽在这上面，已修正。

**必须贴 `_check.py` 的完整原始输出，并显示「结果: 全过 ✓」。**

## 八、交付时说明

1. 文件路径
2. 正文字数
3. 五条自查的实际输出
4. 你新引入的术语（供主控合并进 glossary）
5. 你**没把握**的地方（宁可标出来，不要糊过去）
